MILESTONE=M08
STATUS=PARTIAL
BASE_BRANCH=feat/22-typescript-sdk-completion
BASE_SHA=da95098
FINAL_BRANCH=feat/23-webhook-sdk-lifecycle
FINAL_SHA=SEE_GIT_HEAD_FOR_FINAL_MILESTONE_COMMIT
PULL_REQUEST=NOT_OPENED_LOCAL_ONLY
FILES_CHANGED=packages/webhook-sdk/src/index.ts; packages/webhook-sdk/test/webhook.test.ts; packages/webhook-sdk/README.md; docs/evidence/M08-webhook-sdk-lifecycle.md
CONTRACTS_CHANGED=NO
PUBLIC_BEHAVIOR_CHANGED=YES - @codestra/webhook-sdk now exposes scoped processing claims, processing leases, processed and failed terminal states, scoped replay keys, and a verifyAndClaimWebhookProcessing helper while preserving the existing verifyWebhook and verifyAndClaimWebhook APIs.
SECURITY_BOUNDARY_CHANGED=YES - webhook replay identity can now include tenant ID, endpoint ID, signer type, and event ID, preventing event ID collisions across tenants/endpoints and allowing consumers to fail closed on concurrent processing or completed replay.
TESTS_ADDED=YES - webhook SDK tests cover scoped processing claims, processed replay rejection, concurrent processing rejection, expired lease crash recovery, failed outcome recording, and scope/event mismatch rejection.
LOCAL_COMMANDS=pnpm --filter @codestra/webhook-sdk typecheck; pnpm --filter @codestra/webhook-sdk test; pnpm contracts:validate; pnpm contracts:validate:negative; pnpm build; pnpm typecheck; pnpm workflows:validate; pnpm pacts:validate; pnpm --filter @codestra/social-sdk test:pact; pnpm test:coverage; git diff --check
LOCAL_RESULTS=Webhook SDK typecheck passed. Webhook SDK tests passed with 1 file and 8 tests. Contract validation and negative fixtures passed. Full repo build and typecheck passed across 8 packages. Workflow validation passed for 5 workflows. Pact validation passed for 1 Pact file. Social SDK Pact tests passed with 1 file and 5 tests. Full coverage passed with 8 test files and 44 tests; webhook-sdk src/index.ts reported 85.04% line coverage and 67.96% branch coverage. git diff --check passed with only Windows LF-to-CRLF warnings.
WORKFLOW_RUNS=NOT_AVAILABLE_LOCALLY
ARTIFACTS=WebhookProcessingStore interface; InMemoryWebhookProcessingStore conformance model; scoped webhook processing helper; processing lifecycle README example; lifecycle regression tests
EXTERNAL_DEPENDENCIES=Production consumers must implement WebhookProcessingStore using an atomic durable store with conditional claim, lease-token compare-and-set completion/failure, expiry-based crash recovery, and long enough processed retention for replay defense. Deployed webhook delivery verification, endpoint identity, and GitHub required-check evidence require external infrastructure and repository access.
KNOWN_LIMITATIONS=The in-memory processing store is a local conformance model only. It does not provide cross-process durability, persistent replay retention, or database/Redis atomicity guarantees by itself.
ROLLBACK=Revert the M08 commit to restore the previous signature verification and simple replay claim API.
NEXT_MILESTONE=M09
