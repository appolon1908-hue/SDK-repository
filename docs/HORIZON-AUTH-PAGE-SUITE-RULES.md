# Horizon authentication and suite rules — superseded

Codestra Orbit V2 is the sole authentication-page, page-shell, footer, color, and suite-design authority.

Use [`ORBIT-AUTH-PAGE-SUITE-RULES.md`](ORBIT-AUTH-PAGE-SUITE-RULES.md) and a repository-local `orbit/suite.json` manifest validated by the exact-SHA Orbit action.

Temporary Horizon imports and attributes exist only as a migration compatibility layer. They do not authorize a separate theme, external authentication values, light mode, blue primary actions, gradients, glass effects, glow, large radii, hard-coded social URLs, or source changes to vendor authentication internals.

No production deployment or identity configuration change is authorized by this record.
