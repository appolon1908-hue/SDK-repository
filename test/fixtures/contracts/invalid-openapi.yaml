openapi: 3.1.0
info:
  title: Invalid unresolved reference fixture
  version: 0.0.0
servers:
  - url: https://api.invalid.test
paths:
  /broken:
    get:
      operationId: brokenReference
      responses:
        '200':
          description: Invalid fixture
          content:
            application/json:
              schema:
                $ref: './missing-schema.yaml#/Broken'
