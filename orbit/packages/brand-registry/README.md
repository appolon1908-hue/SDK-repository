# @corporate/brand-registry

Canonical Codestra naming, identity, domain, footer-attribution, return-URL, and social-link validation. A generated hostname is only a naming candidate; activation still requires registry, DNS, TLS, and routing evidence.
