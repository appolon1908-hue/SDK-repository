# @corporate/design-tokens

The single machine-readable Codestra Orbit color, typography, spacing, geometry, motion, and viewport authority. Applications import the package; they do not redefine these values locally.
